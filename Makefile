#
# Standard MPLAB X top-level Makefile.
# Delegates to the generated nbproject/Makefile-impl.mk.
#

# build
build: .build-post

.build-pre:

.build-post: .build-impl

# clean
clean: .clean-post

.clean-pre:

.clean-post: .clean-impl

# clobber
clobber: .clobber-post

.clobber-pre:

.clobber-post: .clobber-impl

# all
all: .all-post

.all-pre:

.all-post: .all-impl

# dependency checking support
.depcheck-impl:

# configuration validation
.validate-impl:

include nbproject/Makefile-impl.mk

include nbproject/Makefile-variables.mk
